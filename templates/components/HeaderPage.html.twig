<div {{ attributes.defaults({class: 'border-bottom py-5 py-lg-6 py-xxl-7'}) }}>
    <div class="container">
        <header>
            <h1 class="mb-0 h2 fw-bold text-uppercase">{{ title }}</h1>
        </header>
        
        <div class="mt-2">
            {% block body %}{% endblock %}
        </div>

        <footer class="mt-3">
            {% block footer %}{% endblock %}
        </footer>
    </div>
</div>
